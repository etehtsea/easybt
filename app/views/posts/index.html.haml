%h1 Listing posts

%table
  %tr
    %th Title
    %th Body
    %th
    %th
    %th

  - collection.each do |post|
    %tr
      %td= link_to post.title, post
      %td= post.body
      %td= link_to 'Edit', edit_resource_path(post)
      %td= link_to 'Destroy', post, :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New Post', new_resource_path
